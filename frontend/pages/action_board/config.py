from datetime import datetime


# ── 비즈니스 로직 ─────────────────────────────────────────────────────────────
# 실제 구독자 모집단 / 샘플 크기 비율.
# 현재는 더미 데이터(2.5만명) 기준이므로 40배로 스케일업.
# 실데이터 연동 시 이 값을 1로 변경하거나 제거하세요.
SCALE_FACTOR = 10
TWD_TO_KRW = 42  # 1 대만 달러 ≈ 42 한국 원

# 고위험 유저 판정 기준
HIGH_RISK_THRESHOLD = 0.60          # 이탈 확률 >= 이 값이면 고위험
EXPIRY_WINDOW_DAYS  = 3            # 만료일까지 남은 일수 <= 이 값이면 대상

# ── 비즈니스 사유 그룹화 (Shared) ──────────────────────────────────────────────────
REASON_GROUPS = {
    "💳 결제 및 구독": [
        "결제 이력 부재(장기 미결제)", "할인 및 가격 혜택 민감도", 
        "자동 결제 미설정", "직접적인 서비스 취소 이력", "결제 수단 이용 특성 변화",
        "멤버십 직접해지", "자동결제 미등록" # Fallback 명칭 포함
    ],
    "🔥 활동 및 참여": [
        "최근 7일간 활동성 급감", "장기 미접속(앱 활동 부재)", "가입 기간 대비 이용성 감소",
        "활동성 저하(추정)" # Fallback 명칭 포함
    ],
    "🎧 이용 습관": [
        "일평균 청취량 및 몰입도 저하", "청취 질(완곡 비율 등) 하락"
    ],
    "👤 유저 프로필": [
        "이용 기간 대비 충성도 하락", "지역적 이용 패턴 변화", "가입 플랫폼별 이용성 차이"
    ],
    "❓ 기타": [
        "복합적 요인(기타 사유)", "기타", "기타/복합"
    ]
}

# ── 모델 경로 (미연동 상태) ───────────────────────────────────────────────────
MODEL_PATH = "models/champion_model.pkl"
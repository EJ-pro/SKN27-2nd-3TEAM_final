from datetime import datetime

# ── 데이터 경로 ───────────────────────────────────────────────────────────────
DATA_DIR = "../data/processed/"
FILES = {
    "members":      "train_members.csv",
    "transactions": "transactions.csv",
    "user_logs":    "user_logs.csv",
}

# ── 시간 시프트 ───────────────────────────────────────────────────────────────
# KKBox 원본 데이터(2017년 기준)를 현재 시점으로 끌어오기 위한 오프셋.
# 수식: (분석 기준 연도 - 원본 데이터 연도) × 365 ≈ 3316일
DATA_ORIGIN_YEAR = 2017
DATA_TARGET_YEAR = 2026
TIME_OFFSET_DAYS = 3316  # 🔥 2017-02-26 -> 2026-03-27 시뮬레이션을 위한 최적값

# ── 비즈니스 로직 ─────────────────────────────────────────────────────────────
# 실제 구독자 모집단 / 샘플 크기 비율.
# 현재는 더미 데이터(2.5만명) 기준이므로 40배로 스케일업.
# 실데이터 연동 시 이 값을 1로 변경하거나 제거하세요.
SCALE_FACTOR = 40
TWD_TO_KRW = 42  # 1 대만 달러 ≈ 42 한국 원

# 고위험 유저 판정 기준
HIGH_RISK_THRESHOLD = 0.70          # 이탈 확률 >= 이 값이면 고위험
EXPIRY_WINDOW_DAYS  = 3             # 만료일까지 남은 일수 <= 이 값이면 대상

# 이탈 주원인 (TODO: SHAP 변수 중요도로 교체)
CHURN_REASONS = {
    "자동결제 해지": 48,
    "청취 권태기":   24,
    "장기 미접속":   18,
    "기타":          10,
}

# ── 모델 경로 (미연동 상태) ───────────────────────────────────────────────────
MODEL_PATH = "models/champion_model.pkl"